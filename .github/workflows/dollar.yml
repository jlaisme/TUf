name: question

on:
  push:

jobs:
  companion:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        expression: ["slippery", "observation", "trumpet", "neighbour", "retire"]
        auxiliary: ["pure", "indication", "optimistic", "gym"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar Zy.jar
